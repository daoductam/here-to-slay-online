version: '3.8'

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    restart: always
    environment:
      - PORT=4000
      - CLIENT_URL=https://jingxianlau.github.io # Allow GitHub Pages client
      # You can add other env vars here if needed
    networks:
      - game-network

  caddy:
    image: caddy:2-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - server
    networks:
      - game-network

volumes:
  caddy_data:
  caddy_config:

networks:
  game-network:
    driver: bridge
